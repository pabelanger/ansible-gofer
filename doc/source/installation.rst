============
Installation
============

At the command line::

    $ pip install ansible-gofer

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ansible-gofer
    $ pip install ansible-gofer
